## 项目结构修改
项目主目录在
```bash
ns-3.45/contrib/opengym/examples/my-open-gym
```
src目录在
```bash
ns3.45/src/generic-topology
ns3.45/src/pfc
```

## 项目文件说明

ecmpConfig.txt: 拓扑配置的初始化文件，ecmpProbability.txt负责更新

env.yml: 环境配置文件，目前没有用到

FbHdp_distribution.txt：生成流量所需的分布文件

mymodel.py: 强化学习模型配置文件，对应超参数未启用


sim.cc：主仿真程序，负责配置网络拓扑和运行环境，集成OpenGym接口实现与强化学习的交互，支持PFC和ECMP配置(使用yky的PFC示例)

rl-env.h、rl-env.cc：强化学习环境，定义自定义Gym环境类MyGymEnv，实现观测空间和动作空间定义，负责收集网络性能指标（吞吐量、延迟、丢包率等）：
```
GetObservation()使用来自module.cc中代码，启用generic-topology的接口模块
```

myns3env.py：Python环境封装，继承自ns3gym.Ns3Env，定义了观测和动作的数据转换逻辑，实现奖励函数计算

train.py：训练脚本，集成回调函数和模型保存，此处有超参数配置

auto.txt:网络拓扑定义


## 项目文件修改记录

ns-3.45/contrib/opengym/examples/CmakeLists.txt

添加my-open-gym例子
```cpp
build_lib_example(
  NAME my-open-gym
  SOURCE_FILES my-open-gym/sim.cc
               my-open-gym/rl-env.cc
  LIBRARIES_TO_LINK
    ${libapplications}
    ${libcore}
    ${libflow-monitor}
    ${libinternet}
    ${libopengym}
    ${libpoint-to-point-layout}
    ${libptp}
    ${libpfc}
    ${libgeneric-topology}
    # ${libnetwork}
    # ${libpoint-to-point}
    # ${libtraffic-control}
    # ${libcsma}
    # ${libmobility}
    # ${libinternet-apps}
    # ${libnetanim}
)
```

### ecmpProb格式修改，需要修改动作空间支持，目前还没好

```
# 每个节点一行：nodeId: w1 w2 ...
# 仅从当前工作目录读取；当某节点某一跳有 N 条候选路径，就取前 N 个权重。
# 控制候选路径权重和为10

# === Core 层,16个节点,0到15===
# core 层一般某个目的只会有 1 条下一跳；这里配置 1:1 仅用于有 2 条候选时也平均
0: 1 1
...
15: 1 1

# === Aggregation 层,80个节点,16到95===
# agg->agg 通常有 2 条候选（上行到两个 agg）
16:5 5
...
95:3 7

# === Edge 层,320个节点,96到415===
# edge->agg 通常有 4 条候选（上行到两个 agg）
96:1 2 3 4
...
415:1 1 2 5

# === Host 层===
# 主机不参与路由转发，可不配置；如需占位可写单个权重（不会被用到）
```

## src对应修改

### 两个Cmakelists.txt文件
 
pfc：
```cpp
build_lib(
    LIBNAME pfc
    SOURCE_FILES
        pfc-header.cc
        qbb-net-device.cc
        qbb-point-to-point-helper.cc
    HEADER_FILES
        pfc-header.h
        qbb-net-device.h
        qbb-point-to-point-helper.h
    LIBRARIES_TO_LINK
        ${libcore}
        ${libnetwork}
        ${libinternet}
        ${libpoint-to-point}
        ${libapplications}
        ${libflow-monitor}
        ${libtraffic-control}
        ${libcsma}
        ${libmobility}
        ${libinternet-apps}
        ${libnetanim}
)
```

generic-topology：

加入PFC对应lib：
```cpp
build_lib(
    LIBNAME generic-topology
    SOURCE_FILES
        model/topo-traffic-builder.cc
        model/sim-stats-collector.cc
    HEADER_FILES
        model/common-defs.h
        model/topo-traffic-builder.h
        model/sim-stats-collector.h
    LIBRARIES_TO_LINK
        ${libcore}
        ${libnetwork}
        ${libinternet}
        ${libpoint-to-point}
        ${libapplications}
        ${libflow-monitor}
        ${libpfc}
    TEST_SOURCES
        test/generic-topology-test-suite.cc
)
```

### 修改生成拓扑文件topo-traffic-builder.cc:

将p2p换成qbb：
```cpp
        //换成qbbnetdevice以支持PFC
        QbbPointToPointHelper qbb;
        qbb.SetChannelAttribute("Delay", StringValue(ds.str()));
        qbb.SetDeviceAttribute("DataRate", StringValue(bs.str()));
        //p2p.SetChannelAttribute("Delay", StringValue(ds.str()));
        //p2p.SetDeviceAttribute("DataRate", StringValue(bs.str()));
        
        NodeContainer nc(m_nodes.Get(n1), m_nodes.Get(n2));
        // NetDeviceContainer ndc = p2p.Install(nc);
        NetDeviceContainer ndc = qbb.Install(nc);
        address.Assign(ndc); address.NewNetwork();
        
        Ptr<QbbNetDevice> d0 = DynamicCast<QbbNetDevice>(ndc.Get(0));
        Ptr<QbbNetDevice> d1 = DynamicCast<QbbNetDevice>(ndc.Get(1));
        // Ptr<PointToPointNetDevice> d0 = DynamicCast<PointToPointNetDevice>(ndc.Get(0));
        // Ptr<PointToPointNetDevice> d1 = DynamicCast<PointToPointNetDevice>(ndc.Get(1));
```
### 同样的，修改common-defs.cc：
```cpp
struct LinkInfo {
    uint32_t nodeA;
    uint32_t nodeB;
    Ptr<QbbNetDevice> devA;
    Ptr<QbbNetDevice> devB;
    // Ptr<PointToPointNetDevice> devA;
    // Ptr<PointToPointNetDevice> devB;
    uint64_t dataRateBps;
};
```